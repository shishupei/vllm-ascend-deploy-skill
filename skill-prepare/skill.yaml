name: vllm-deploy-prepare
description: vLLM-Ascend 部署准备 - 获取模型列表、解析文档、处理镜像、生成配置
version: 1.0.0
author: buchuibuhei
triggers:
  - /vllm-deploy-prepare
  - vllm 部署准备
dependencies:
  - curl/wget
  - docker (可选)
output_dir: .vllm-deploy/