apiVersion: v1
kind: Service
metadata:
  name: vllm-service
  namespace: ${NAMESPACE}
spec:
  type: NodePort
  selector:
    app: vllm-deploy
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: ${SERVICE_PORT}