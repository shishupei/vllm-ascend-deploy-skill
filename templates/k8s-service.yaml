apiVersion: v1
kind: Service
metadata:
  name: ${MODEL_NAME}-service
  namespace: ${NAMESPACE}
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: ${MODEL_NAME}
  ports:
    - port: ${SERVICE_PORT}
      targetPort: ${SERVICE_PORT}